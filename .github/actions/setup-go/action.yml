name: Setup Go
description: Set up the Go toolchain and dependency cache for timich-mcp.

runs:
  using: composite
  steps:
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version-file: go.mod
        cache-dependency-path: go.sum
